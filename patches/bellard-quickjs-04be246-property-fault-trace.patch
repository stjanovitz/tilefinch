diff --git a/quickjs.c b/quickjs.c
index 61c64bde..d59e37ce 100644
--- a/quickjs.c
+++ b/quickjs.c
@@ -343,6 +343,11 @@ struct JSRuntime {
        keeps upstream 1.5x growth for controlled comparisons. */
     size_t fast_array_growth_byte_limit;
 
+#ifdef CONFIG_PROPERTY_FAULT_TRACE
+    uint32_t property_fault_trace_count;
+    uint32_t property_fault_trace_limit;
+#endif
+
     int class_count;    /* size of class_array */
     JSClass *class_array;
 
@@ -2292,6 +2297,16 @@ JSRuntime *JS_NewRuntime(void)
     return JS_NewRuntime2(&def_malloc_funcs, NULL);
 }
 
+#ifdef CONFIG_PROPERTY_FAULT_TRACE
+/* Diagnostic-only API kept out of quickjs.h so production embedders do not
+   acquire a non-upstream ABI surface. */
+void JS_SetPropertyFaultTraceLimit(JSRuntime *rt, uint32_t limit)
+{
+    rt->property_fault_trace_count = 0;
+    rt->property_fault_trace_limit = limit;
+}
+#endif
+
 void JS_SetMemoryLimit(JSRuntime *rt, size_t limit)
 {
     rt->malloc_ctx.malloc_state.malloc_limit = limit;
@@ -7760,6 +7775,192 @@ static int find_line_num(JSContext *ctx, JSFunctionBytecode *b,
     return 0;
 }
 
+#ifdef CONFIG_PROPERTY_FAULT_TRACE
+static const char *property_fault_tag_name(int tag)
+{
+    switch (tag) {
+    case JS_TAG_INT: return "int";
+    case JS_TAG_BOOL: return "bool";
+    case JS_TAG_NULL: return "null";
+    case JS_TAG_UNDEFINED: return "undefined";
+    case JS_TAG_FLOAT64: return "float64";
+    case JS_TAG_STRING: return "string";
+    case JS_TAG_STRING_ROPE: return "string-rope";
+    case JS_TAG_SYMBOL: return "symbol";
+    case JS_TAG_OBJECT: return "object";
+    case JS_TAG_BIG_INT: return "bigint";
+    case JS_TAG_EXCEPTION: return "exception";
+    case JS_TAG_UNINITIALIZED: return "uninitialized";
+    default: return "internal";
+    }
+}
+
+static void trace_property_fault(JSContext *ctx, int kind,
+                                 JSFunctionBytecode *b,
+                                 const uint8_t *pc, int opcode,
+                                 JSValueConst base, JSValueConst key,
+                                 JSAtom atom, const JSValue *stack_base,
+                                 const JSValue *sp, const JSValue *arg_base,
+                                 int arg_count, const JSValue *var_base,
+                                 int var_count)
+{
+    JSRuntime *rt = ctx->rt;
+    char filename_buf[192], function_buf[96], atom_buf[96];
+    const char *filename = "<stripped>";
+    const char *function = "<anonymous>";
+    const char *property = "<computed>";
+    uint32_t pc_offset = (uint32_t)(pc - b->byte_code_buf);
+    int col = 0, line = find_line_num(ctx, b, pc_offset, &col);
+    int i, depth = (int)(sp - stack_base);
+    const char *trace_mode = getenv("TILEFINCH_TRACE_JS_PROPERTY_FAULTS");
+
+    if (trace_mode != NULL) {
+        if (strcmp(trace_mode, "absent") == 0 && kind != 2)
+            return;
+        if (strcmp(trace_mode, "fault") == 0 && kind != 0)
+            return;
+    }
+
+    if (rt->property_fault_trace_limit == 0
+        || rt->property_fault_trace_count >= rt->property_fault_trace_limit
+        || !b->has_debug || b->debug.filename == JS_ATOM_NULL)
+        return;
+    filename = JS_AtomGetStr(ctx, filename_buf, sizeof(filename_buf),
+                             b->debug.filename);
+    /* Embedded bootstrap bytecode deliberately strips source metadata. Its
+       optional internal property probes otherwise consume the complete
+       diagnostic budget before author code executes. */
+    if (strcmp(filename, "<stripped>") == 0
+        || strcmp(filename, "<worker-platform>") == 0)
+        return;
+    rt->property_fault_trace_count++;
+    if (b->func_name != JS_ATOM_NULL)
+        function = JS_AtomGetStr(ctx, function_buf, sizeof(function_buf),
+                                 b->func_name);
+    if (atom != JS_ATOM_NULL) {
+        property = JS_AtomGetStr(ctx, atom_buf, sizeof(atom_buf), atom);
+    } else if (JS_VALUE_GET_TAG(key) == JS_TAG_INT) {
+        snprintf(atom_buf, sizeof(atom_buf), "%d", JS_VALUE_GET_INT(key));
+        property = atom_buf;
+    } else if (JS_VALUE_GET_TAG(key) == JS_TAG_STRING) {
+        JSString *string = JS_VALUE_GET_STRING(key);
+        size_t written = (size_t)snprintf(atom_buf, sizeof(atom_buf),
+                                          "<string:%u:", string->len);
+        uint32_t at;
+        for (at = 0; at < string->len && at < 40
+             && written + 2 < sizeof(atom_buf); at++) {
+            int ch = string_get(string, at);
+            atom_buf[written++] = ch >= 0x20 && ch < 0x7f ? (char)ch : '?';
+        }
+        atom_buf[written++] = '>';
+        atom_buf[written] = '\0';
+        property = atom_buf;
+    }
+    fprintf(stderr,
+            "quickjs-property-%s seq=%u source=\"%.*s\" line=%d col=%d "
+            "function=\"%.*s\" pc=%u opcode=%d base=%s key=%s property=\"%.*s\" depth=%d\n",
+            kind == 2 ? "absent" : kind == 1 ? "miss" : "fault", rt->property_fault_trace_count,
+            180, filename, line, col,
+            84, function, pc_offset, opcode,
+            property_fault_tag_name(JS_VALUE_GET_TAG(base)),
+            property_fault_tag_name(JS_VALUE_GET_TAG(key)), 84, property,
+            depth);
+    fprintf(stderr, "quickjs-property-stack top-tags=");
+    for (i = 0; i < 8 && i < depth; i++)
+        fprintf(stderr, "%s%s", i == 0 ? "" : ",",
+                property_fault_tag_name(JS_VALUE_GET_TAG(sp[-1 - i])));
+    fprintf(stderr, " arg-tags=");
+    for (i = 0; i < 8 && i < arg_count; i++)
+        fprintf(stderr, "%s%s", i == 0 ? "" : ",",
+                property_fault_tag_name(JS_VALUE_GET_TAG(arg_base[i])));
+    fprintf(stderr, " arg-values=");
+    for (i = 0; i < 16 && i < arg_count; i++) {
+        int tag = JS_VALUE_GET_TAG(arg_base[i]);
+        fprintf(stderr, "%s%s", i == 0 ? "" : ",",
+                property_fault_tag_name(tag));
+        if (tag == JS_TAG_INT || tag == JS_TAG_BOOL)
+            fprintf(stderr, ":%d", JS_VALUE_GET_INT(arg_base[i]));
+        else if (tag == JS_TAG_FLOAT64)
+            fprintf(stderr, ":%.17g", JS_VALUE_GET_FLOAT64(arg_base[i]));
+        else if (tag == JS_TAG_STRING)
+            fprintf(stderr, ":%u",
+                    JS_VALUE_GET_STRING(arg_base[i])->len);
+    }
+    fprintf(stderr, " var-tags=");
+    for (i = 0; i < 8 && i < var_count; i++)
+        fprintf(stderr, "%s%s", i == 0 ? "" : ",",
+                property_fault_tag_name(JS_VALUE_GET_TAG(var_base[i])));
+    fprintf(stderr, " code=");
+    for (i = -8; i < 8; i++) {
+        int offset = (int)pc_offset + i;
+        if (offset >= 0 && offset < b->byte_code_len)
+            fprintf(stderr, "%s%02x", i == -8 ? "" : ":",
+                    b->byte_code_buf[offset]);
+    }
+    fprintf(stderr, "\n");
+}
+
+static void trace_call_fault(JSContext *ctx, JSFunctionBytecode *b,
+                             const uint8_t *pc, int opcode,
+                             JSValueConst callee, JSValueConst receiver,
+                             int call_argc, const JSValue *stack_base,
+                             const JSValue *sp)
+{
+    JSRuntime *rt = ctx->rt;
+    char filename_buf[192], function_buf[96];
+    const char *filename = "<stripped>";
+    const char *function = "<anonymous>";
+    uint32_t pc_offset = (uint32_t)(pc - b->byte_code_buf);
+    int col = 0, line = find_line_num(ctx, b, pc_offset, &col);
+    int i, depth = (int)(sp - stack_base);
+
+    if (rt->property_fault_trace_limit == 0
+        || rt->property_fault_trace_count >= rt->property_fault_trace_limit
+        || !b->has_debug || b->debug.filename == JS_ATOM_NULL)
+        return;
+    filename = JS_AtomGetStr(ctx, filename_buf, sizeof(filename_buf),
+                             b->debug.filename);
+    if (strcmp(filename, "<stripped>") == 0
+        || strcmp(filename, "<worker-platform>") == 0)
+        return;
+    rt->property_fault_trace_count++;
+    if (b->func_name != JS_ATOM_NULL)
+        function = JS_AtomGetStr(ctx, function_buf, sizeof(function_buf),
+                                 b->func_name);
+    fprintf(stderr,
+            "quickjs-call-fault seq=%u source=\"%.*s\" line=%d col=%d "
+            "function=\"%.*s\" pc=%u opcode=%d callee=%s receiver=%s "
+            "argc=%d depth=%d top-tags=",
+            rt->property_fault_trace_count, 180, filename, line, col,
+            84, function, pc_offset, opcode,
+            property_fault_tag_name(JS_VALUE_GET_TAG(callee)),
+            property_fault_tag_name(JS_VALUE_GET_TAG(receiver)),
+            call_argc, depth);
+    for (i = 0; i < 8 && i < depth; i++)
+        fprintf(stderr, "%s%s", i == 0 ? "" : ",",
+                property_fault_tag_name(JS_VALUE_GET_TAG(sp[-1 - i])));
+    if (JS_VALUE_GET_TAG(callee) == JS_TAG_STRING) {
+        JSString *string = JS_VALUE_GET_STRING(callee);
+        uint32_t at, limit = string->len < 64 ? string->len : 64;
+        fprintf(stderr, " callee-string=\"");
+        for (at = 0; at < limit; at++) {
+            int ch = string_get(string, at);
+            fputc(ch >= 0x20 && ch < 0x7f && ch != '"' && ch != '\\'
+                      ? ch : '?', stderr);
+        }
+        fprintf(stderr, "\" callee-length=%u", string->len);
+    }
+    fprintf(stderr, " code=");
+    for (i = -8; i < 8; i++) {
+        int offset = (int)pc_offset + i;
+        if (offset >= 0 && offset < b->byte_code_len)
+            fprintf(stderr, "%s%02x", i == -8 ? "" : ":",
+                    b->byte_code_buf[offset]);
+    }
+    fprintf(stderr, "\n");
+}
+#endif
+
 /* return a string property without executing arbitrary JS code (used
    when dumping the stack trace or in debug print). */
 static const char *get_prop_string(JSContext *ctx, JSValueConst obj, JSAtom prop)
@@ -18510,6 +18694,11 @@ static JSValue JS_CallInternal(JSContext *caller_ctx, JSValueConst func_obj,
             has_call_argc:
                 call_argv = sp - call_argc;
                 sf->cur_pc = pc;
+#ifdef CONFIG_PROPERTY_FAULT_TRACE
+                if (unlikely(!JS_IsFunction(ctx, call_argv[-1])))
+                    trace_call_fault(ctx, b, pc, opcode, call_argv[-1],
+                                     JS_UNDEFINED, call_argc, stack_buf, sp);
+#endif
                 ret_val = JS_CallInternal(ctx, call_argv[-1], JS_UNDEFINED,
                                           JS_UNDEFINED, call_argc, call_argv, 0);
                 if (unlikely(JS_IsException(ret_val)))
@@ -18546,6 +18735,11 @@ static JSValue JS_CallInternal(JSContext *caller_ctx, JSValueConst func_obj,
                 pc += 2;
                 call_argv = sp - call_argc;
                 sf->cur_pc = pc;
+#ifdef CONFIG_PROPERTY_FAULT_TRACE
+                if (unlikely(!JS_IsFunction(ctx, call_argv[-1])))
+                    trace_call_fault(ctx, b, pc, opcode, call_argv[-1],
+                                     call_argv[-2], call_argc, stack_buf, sp);
+#endif
                 ret_val = JS_CallInternal(ctx, call_argv[-1], call_argv[-2],
                                           JS_UNDEFINED, call_argc, call_argv, 0);
                 if (unlikely(JS_IsException(ret_val)))
@@ -19469,10 +19663,23 @@ static JSValue JS_CallInternal(JSContext *caller_ctx, JSValueConst func_obj,
                 } else {                                                \
                 name ## _slow_path:                                     \
                     sf->cur_pc = pc;                                    \
+                    if (unlikely(JS_IsNull(obj) || JS_IsUndefined(obj))) { \
+                        trace_property_fault(ctx, 0, b, pc, opcode, obj, \
+                                             JS_UNDEFINED, atom,        \
+                                             stack_buf, sp, arg_buf,    \
+                                             sf->arg_count, var_buf,    \
+                                             b->var_count);             \
+                    }                                                   \
                     val = JS_GetPropertyInternal(ctx, obj, atom, sp[-1], 0); \
                     if (unlikely(JS_IsException(val)))                  \
                         goto exception;                                 \
                 }                                                       \
+                if (unlikely(JS_IsUndefined(val) && JS_IsObject(sp[-1]))) \
+                    trace_property_fault(ctx, 1, b, pc, opcode, sp[-1], \
+                                         JS_UNDEFINED, atom,            \
+                                         stack_buf, sp, arg_buf,       \
+                                         sf->arg_count, var_buf,       \
+                                         b->var_count);                 \
                 if (keep) {                                             \
                     *sp++ = val;                                        \
                 } else {                                                \
@@ -19531,7 +19738,14 @@ static JSValue JS_CallInternal(JSContext *caller_ctx, JSValueConst func_obj,
                     sp -= 2;
                 } else {
                 put_field_slow_path:
                     sf->cur_pc = pc;
+#ifdef CONFIG_PROPERTY_FAULT_TRACE
+                    if (unlikely(JS_IsNull(obj) || JS_IsUndefined(obj)))
+                        trace_property_fault(ctx, 0, b, pc, opcode, obj,
+                                             JS_UNDEFINED, atom, stack_buf, sp,
+                                             arg_buf, sf->arg_count, var_buf,
+                                             b->var_count);
+#endif
                     ret = JS_SetPropertyInternal(ctx, obj, atom, sp[-1], obj,
                                                  JS_PROP_THROW_STRICT);
                     JS_FreeValue(ctx, obj);
@@ -19719,5 +19926,5 @@ static JSValue JS_CallInternal(JSContext *caller_ctx, JSValueConst func_obj,
 #define GET_ARRAY_EL_INLINE(name, keep)                                 \
             {                                                           \
-                JSValue val, obj, prop;                                 \
+                JSValue val, obj, prop, trace_prop;                     \
                 JSObject *p;                                            \
                 uint32_t idx;                                           \
@@ -19734,15 +19941,30 @@ static JSValue JS_CallInternal(JSContext *caller_ctx, JSValueConst func_obj,
                     val = JS_DupValue(ctx, p->u.array.u.values[idx]);   \
                 } else {                                                \
                     name ## _slow_path:                                 \
+                    trace_prop = JS_DupValue(ctx, prop);                \
                     sf->cur_pc = pc;                                    \
+                    if (unlikely(JS_IsNull(obj) || JS_IsUndefined(obj))) { \
+                        trace_property_fault(ctx, 0, b, pc, opcode, obj, prop, \
+                                             JS_ATOM_NULL, stack_buf, sp, \
+                                             arg_buf, sf->arg_count,     \
+                                             var_buf, b->var_count);    \
+                    }                                                   \
                     val = JS_GetPropertyValue(ctx, obj, prop);          \
                     if (unlikely(JS_IsException(val))) {                \
+                        JS_FreeValue(ctx, trace_prop);                   \
                         if (keep)                                       \
                             sp[-1] = JS_UNDEFINED;                      \
                         else                                            \
                             sp--;                                       \
                         goto exception;                                 \
                     }                                                   \
+                    if (unlikely(JS_IsUndefined(val) && JS_IsObject(obj))) \
+                        trace_property_fault(ctx, 1, b, pc, opcode, obj, \
+                                             trace_prop, JS_ATOM_NULL,  \
+                                             stack_buf, sp, arg_buf,   \
+                                             sf->arg_count, var_buf,   \
+                                             b->var_count);             \
+                    JS_FreeValue(ctx, trace_prop);                      \
                 }                                                       \
                 if (keep) {                                             \
                     sp[-1] = val;                                       \
@@ -19787,6 +20009,11 @@ static JSValue JS_CallInternal(JSContext *caller_ctx, JSValueConst func_obj,
                     default:
                         /* must be tested before JS_ToPropertyKey */
                         if (unlikely(JS_IsUndefined(sp[-2]) || JS_IsNull(sp[-2]))) {
+                            trace_property_fault(ctx, 0, b, pc, opcode, sp[-2],
+                                                 sp[-1], JS_ATOM_NULL,
+                                                 stack_buf, sp, arg_buf,
+                                                 sf->arg_count, var_buf,
+                                                 b->var_count);
                             JS_ThrowTypeError(ctx, "value has no property");
                             goto exception;
                         }
@@ -19798,11 +20025,24 @@ static JSValue JS_CallInternal(JSContext *caller_ctx, JSValueConst func_obj,
                         sp[-1] = ret_val;
                         break;
                     }
+                    if (unlikely(JS_IsNull(sp[-2]) || JS_IsUndefined(sp[-2]))) {
+                        trace_property_fault(ctx, 0, b, pc, opcode, sp[-2],
+                                             sp[-1], JS_ATOM_NULL,
+                                             stack_buf, sp, arg_buf,
+                                             sf->arg_count, var_buf,
+                                             b->var_count);
+                    }
                     sf->cur_pc = pc;
                     val = JS_GetPropertyValue(ctx, sp[-2], JS_DupValue(ctx, sp[-1]));
                     if (unlikely(JS_IsException(val)))
                         goto exception;
                 }
+                if (unlikely(JS_IsUndefined(val) && JS_IsObject(sp[-2])))
+                    trace_property_fault(ctx, 1, b, pc, opcode, sp[-2],
+                                         sp[-1], JS_ATOM_NULL,
+                                         stack_buf, sp, arg_buf,
+                                         sf->arg_count, var_buf,
+                                         b->var_count);
                 *sp++ = val;
             }
             BREAK;
@@ -19911,6 +20162,13 @@ static JSValue JS_CallInternal(JSContext *caller_ctx, JSValueConst func_obj,
                 } else {
                 put_array_el_slow_path:
                     sf->cur_pc = pc;
+#ifdef CONFIG_PROPERTY_FAULT_TRACE
+                    if (unlikely(JS_IsNull(sp[-3]) || JS_IsUndefined(sp[-3])))
+                        trace_property_fault(ctx, 0, b, pc, opcode, sp[-3],
+                                             sp[-2], JS_ATOM_NULL, stack_buf, sp,
+                                             arg_buf, sf->arg_count, var_buf,
+                                             b->var_count);
+#endif
                     ret = JS_SetPropertyValue(ctx, sp[-3], sp[-2], sp[-1], JS_PROP_THROW_STRICT);
                     JS_FreeValue(ctx, sp[-3]);
                     sp -= 3;
@@ -20642,8 +20865,31 @@ static JSValue JS_CallInternal(JSContext *caller_ctx, JSValueConst func_obj,
         CASE(OP_in):
+#ifdef CONFIG_PROPERTY_FAULT_TRACE
+            {
+                JSValue trace_key = JS_DupValue(ctx, sp[-2]);
+                JSValue trace_base = JS_DupValue(ctx, sp[-1]);
+                int in_result;
+                sf->cur_pc = pc;
+                in_result = js_operator_in(ctx, sp);
+                if (in_result) {
+                    JS_FreeValue(ctx, trace_key);
+                    JS_FreeValue(ctx, trace_base);
+                    goto exception;
+                }
+                if (!JS_ToBool(ctx, sp[-2]))
+                    trace_property_fault(ctx, 2, b, pc, opcode, trace_base,
+                                         trace_key, JS_ATOM_NULL, stack_buf,
+                                         sp, arg_buf, sf->arg_count, var_buf,
+                                         b->var_count);
+                JS_FreeValue(ctx, trace_key);
+                JS_FreeValue(ctx, trace_base);
+                sp--;
+            }
+#else
             sf->cur_pc = pc;
             if (js_operator_in(ctx, sp))
                 goto exception;
             sp--;
+#endif
             BREAK;
         CASE(OP_private_in):
             sf->cur_pc = pc;
