DOM order deliberately disagrees with viewport order. Hidden descendants and
an offscreen lazy image must remain unfetched until reveal/scroll, while two
visible images precede an eager offscreen image. All URLs are synthetic.
