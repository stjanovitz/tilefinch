# Deferred image timeout

The ordinary interactive-image journey, with the hero response timing out
after twenty pumps. Tail images still succeed. This proves that an optional
resource failure neither retires the incumbent page nor strands the queue.
Synthetic URLs only; the v10 request policy fields remain enforced.
